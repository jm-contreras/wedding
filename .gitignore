# Internal-only material — never deploy to the public site
docs/

# Raw source photos/videos — keep locally, only web-optimized
# copies in imgs/our-story/ are committed and deployed
imgs/Adventures Together/
imgs/Going Steady/
imgs/How It All Began/
imgs/The Big Question/
imgs/ProposalHydra0062.png
imgs/ruins.png
imgs/antigua.png
imgs/hotel.png
imgs/tuktuk.png

# Local-preview symlinks so edits/ renders standalone in a browser.
# They point at the root copies of assets that edits/ does not duplicate.
edits/imgs
edits/favicon.svg

# OS / tooling
.DS_Store
.vercel/
.vercel
